[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = [
  "cd backend && npm ci --omit=dev"
]

[phases.build]
cmds = ["echo 'Build completed - No additional build step required'"]

[start]
cmd = "cd backend && node src/server.js"
